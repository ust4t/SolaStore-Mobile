import { Platform } from "react-native"


const cartIcon=Platform.select({
    ios:"cart-outline",
    android:"cart-outline"
})


const starIcon=Platform.select({
    ios:"ios-star",
    android:"md-star"
})

const cancelIcon=Platform.select({
    ios:"ios-close",
    android:"md-close"
})

const addIcon=Platform.select({
    ios:"ios-add",
    android:"md-add"
})

const removeIcon=Platform.select({
    ios:"ios-remove",
    android:"md-remove"
})

const homeIcon=Platform.select({
    ios:"ios-home",
    android:"md-home"
})

const searchIcon=Platform.select({
    ios:"ios-search",
    android:"md-search"
})

const giftIcon=Platform.select({
    ios:"ios-gift-outline",
    android:"md-gift-outline"
})

const personIcon=Platform.select({
    ios:"ios-person",
    android:"md-person"
})

const caretIcon=Platform.select({
    ios:"ios-caret-down",
    android:"md-caret-down"
})

const watchIcon=Platform.select({
    ios:"ios-watch-outline",
    android:"md-watch-outline"
})

const cashIcon=Platform.select({
    ios:"ios-cash-outline",
    android:"md-cash-outline"
})

const carIcon=Platform.select({
    ios:"ios-car",
    android:"md-car"
})


const ordersIcon=Platform.select({
    ios:"ios-list",
    android:"md-list"
})

const caretForward=Platform.select({
    ios:"ios-caret-forward-outline",
    android:"md-caret-forward-outline"
})

const readerIcon=Platform.select({
    ios:"ios-reader",
    android:"md-reader"
})


const cardIcon=Platform.select({
    ios:"ios-card",
    android:"md-card"
})

const addressIcon=Platform.select({
    ios:"ios-pin",
    android:"md-pin"
})


const infoIcon=Platform.select({
    ios:"ios-information",
    android:"md-information"
})

const basketIcon=Platform.select({
    ios:"ios-basket",
    android:"md-basket"
})

const starSharIcon=Platform.select({
    ios:"ios-star",
    android:"md-star"
})


const trashIcon=Platform.select({
    ios:"ios-trash",
    android:"md-trash"
})


const locateIcon=Platform.select({
    ios:"ios-locate",
    android:"md-locate"
})


const businessIcon=Platform.select({
    ios:"ios-business",
    android:"md-business"
})


const lockIcon=Platform.select({
    ios:"ios-lock-closed-outline",
    android:"md-lock-closed-outline"
})


const arrowBack=Platform.select({
    ios:"ios-arrow-back",
    android:"md-arrow-back"
})

const locationIcon=Platform.select({
    ios:"ios-location-outline",
    android:"md-location-outline"
})


const checkmarkIcon=Platform.select({
    ios:"ios-checkmark",
    android:"md-checkmark"
})


const walletIcon=Platform.select({
    ios:"ios-wallet-outline",
    android:"md-wallet-outline"
})
const heartIcon=Platform.select({
    ios:"ios-heart",
    android:"md-heart"
})
const heartOutlineIcon=Platform.select({
    ios:"ios-heart-outline",
    android:"md-heart-outline"
})

const appsIcon=Platform.select({
    ios:"ios-apps",
    android:"md-apps"
})
const forwardChevron=Platform.select({
    ios:"ios-chevron-forward",
    android:"md-chevron-forward"
})
const   downChevron=Platform.select({
    ios:"ios-chevron-down",
    android:"md-chevron-down"
})

const   logOutIdon=Platform.select({
    ios:"ios-log-out-outline",
    android:"md-log-out-outline"
})


const   elipsisHorizontalIcon=Platform.select({
    ios:"ios-ellipsis-horizontal",
    android:"md-ellipsis-horizontal"
})


const   trashBinOutline=Platform.select({
    ios:"ios-trash-bin-outline",
    android:"md-trash-bin-outline"
})



const alertIcon=Platform.select({
    ios:"ios-alert",
    android:"md-alert"
})


const filterIcon=Platform.select({
    ios:"ios-filter",
    android:"md-filter"
})

const ellipsisVertical=Platform.select({
    ios:"ios-ellipsis-vertical",
    android:"md-ellipsis-vertical"
})


const optionsIcon=Platform.select({
    ios:"ios-options-outline",
    android:"md-options-outline"
})

const notificationIcon=Platform.select({
    ios:"ios-notifications",
    android:"md-notifications"
})

const eyeIcon=Platform.select({
    ios:"ios-eye",
    android:"md-eye"
})

const eyeOffIcon=Platform.select({
    ios:"ios-eye-off",
    android:"md-eye-off"
})
const settingsIcon=Platform.select({
    ios:"ios-settings",
    android:"md-settings"
})
export  {
    cartIcon,starIcon,cancelIcon,addIcon,removeIcon,homeIcon,searchIcon,giftIcon,personIcon,caretIcon,watchIcon,cashIcon,carIcon,
    ordersIcon,caretForward,readerIcon,cardIcon,addressIcon,infoIcon,basketIcon,starSharIcon,trashIcon,locateIcon,businessIcon,
    lockIcon,arrowBack,locationIcon,checkmarkIcon,walletIcon,heartIcon,appsIcon,heartOutlineIcon,forwardChevron,downChevron,logOutIdon
    ,elipsisHorizontalIcon,trashBinOutline,alertIcon,filterIcon,ellipsisVertical,optionsIcon,notificationIcon,
    eyeIcon,eyeOffIcon,settingsIcon

}